import type { MetaFunction } from "react-router";

export const meta: MetaFunction = () => {
  return [
    { title: "Admin update member page - Newave Store" },
    { name: "description", content: "Admin update member for Newave Store" },
  ];
};
const updateMember = () => {
  return <div>updateMember</div>;
};

export default updateMember;
