import type { MetaFunction } from "react-router";

export const meta: MetaFunction = () => {
  return [
    { title: "Admin add subcategory - Newave Store" },
    { name: "description", content: "Admin add subcategory for Newave Store" },
  ];
};
const addSubCategory = () => {
  return <div>addSubCategory</div>;
};

export default addSubCategory;
