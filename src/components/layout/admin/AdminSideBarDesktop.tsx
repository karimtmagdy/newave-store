// import { useAuth } from "@/hooks/useAuth";

const AdminSideBarDesktop = () => {
  //  const { user } = useAuth();
  return (
    <aside className="hidden w-60 overflow-hidden border md:flex">
      <ul>
        <li>AdminSideBarDesktop</li>
      </ul>
    </aside>
  );
};

export default AdminSideBarDesktop;
